# Задача

Необходимо расширить возможности фреймворка Agentic-development для поддержки claude code и gemini cli, расширить понимание пользователем концепции фреймворка.

Для этого нужно добавить описание в README.md и README.ru.md о том как использовать фреймворк с claude code и gemini cli. А также добавить примеры использования в соответствующие файлы.
## Использование c claude code

Добавить в Installation instructions   инструкцию по установке claude code.
Добавить в примеры использования с claude code.

### Материалы, которые можно использовать для описания

#### Вариант A: Использовать AGENTS.md как есть (рекомендуется)

```text
# 1. Копируем фреймворк
cd /path/to/your-project
cp -r ~/agentic-framework/System ./
cp -r ~/agentic-framework/.agent ./
cp ~/agentic-framework/AGENTS.md ./

# 2. Claude Code ищет CLAUDE.md, создаём алиас
ln -s AGENTS.md CLAUDE.md

# 3. Создаём символическую ссылку для skills (опционально, для совместимости с Cursor)
mkdir -p .claude
ln -s ../.agent/skills .claude/skills

```

#### Вариант B: Адаптировать под специфику проекта

Если вам нужны дополнительные project-specific правила, создайте отдельный файл:
```bash
# Структура:
CLAUDE.md              ← Точка входа (симлинк на AGENTS.md)
AGENTS.md              ← Оркестрация (из фреймворка, неизменный)
.claude/PROJECT.md     ← Ваши project-specific правила
```


CLAUDE.md:

```
# Load Agentic Framework
@AGENTS.md

# Load Project-Specific Rules
@.claude/PROJECT.md
```

.claude/PROJECT.md:

```
# Project: Task Management API

## Tech Stack
- FastAPI 0.104+
- PostgreSQL + SQLAlchemy 2.0
- Redis for caching

## Commands

pytest                  # Run tests
uvicorn app.main:app    # Start server

## Critical Rules
- NEVER commit .env files
- ALWAYS use async DB sessions
- JWT tokens: Access=15min, Refresh=7d
```

## Использование c gemini cli

Добавить в Installation instructions инструкцию по установке gemini cli.
Добавить в примеры использования с gemini cli.

Возможно что-то переиспользовать.

## Расширение концепции

Добавить во все README основное различие @[System/Agents/00_agent_development.md] и @[GEMINI.md] / @[AGENTS.md]. Это необходимо для понимания пользователем концепции 

1. System/Agents/00_agent_development.md
 — Концептуальная основа (The Blueprint)
Этот файл является архитектурной документацией и справочником по тому, как устроена агентная система разработки.

Роль: Описывает "Что это за система" и "Из чего она состоит".
Ключевое содержание:
Определения ролей: Детально расписывает функции каждого агента (Orchestrator, Analyst, Architect, Developer и т.д.), их входные/выходные данные и используемые навыки.
Принципы работы: Объясняет уровни навыков (Skill Tiers), режим task_boundary и философию (Stub-First, Documentation First).
Правила артефактов: Устанавливает стандарты для TASK.md, ARCHITECTURE.md и PLAN.md.
Циклы: Описывает теоретические циклы (Init → Review → Revision).
Назначение: Служит источником истины (Source of Truth) для понимания структуры системы. Если вы хотите узнать, за что отвечает "Архитектор" или какой чек-лист он использует, вы смотрите сюда.
2. 
GEMINI.md / AGENTS.md
 — Операционная инструкция (The Driver)
Этот файл является системным промптом (инструкцией) непосредственно для AI-агента (Оркестратора).

Роль: Говорит агенту "Что делать прямо сейчас" и "Как исполнять правила". Это, по сути, исполняемый код логики поведения агента на естественном языке.
Ключевое содержание:
Императивные команды: Содержит прямые приказы (MUST, CRITICAL INSTRUCTION, NEVER).
Алгоритм действий (The Pipeline): Пошаговая инструкция, которую агент обязан выполнять при получении задачи (Analysis → Architecture → Planning → Development).
Протоколы: Как загружать навыки, как восстанавливать сессию, как выбирать Workflow (Dynamic Dispatch).
Правила поведения: Запрет на кодинг без плана, приоритет инструментов над shell-командами.
Назначение: Превращает концепцию из 
00_agent_development.md
 в обязательные к исполнению действия. Это то, что "программирует" меня (агента) вести себя именно так, как задумано.
Сравнительная таблица
Характеристика	
00_agent_development.md
GEMINI.md
Тип	Документация / Спецификация	Промпт / Инструкция
Стиль	Описательный ("Система состоит из...")	Повелительный ("Ты должен сделать...")
Цель	Объяснить устройство системы	Управлять поведением агента
Для кого	Для разработчика системы и агентов (context)	Для самого AI-агента (system instruction)
Аналогия	Конституция / Устав	Должностная инструкция / Алгоритм
Взаимосвязь: 
GEMINI.md
 обеспечивает исполнение того, что описано в 
00_agent_development.md
. Без первого файла агент не знал бы общей картины и ролей. Без второго — агент знал бы теорию, но не имел бы жесткого алгоритма действий для выполнения задач пользователя.

## 🚀 Практическое использование с Claude Code

### Сценарий 1: Стандартная разработка (полный pipeline)

```bash
cd my-project
claude
```

**Промпт:**
```
Разработай модуль платежей через Stripe

Требования:
- Endpoint для создания checkout session
- Webhook для обработки событий
- Сохранение транзакций в БД
```

**Что происходит автоматически:**

```
Claude (загружает AGENTS.md):
│
├─► Я — Оркестратор. Запускаю Agentic Pipeline...
│
├─► Phase 1: Analysis
│   ├─► Читаю System/Agents/02_analyst_prompt.md
│   ├─► Загружаю skill-requirements-analysis
│   ├─► Создаю docs/TASK.md с детальной спецификацией
│   └─► Проверка через 03_task_reviewer_prompt.md ✅
│
├─► Phase 2: Architecture
│   ├─► Читаю System/Agents/04_architect_prompt.md
│   ├─► Проектирую систему: models, services, webhooks
│   ├─► Создаю docs/ARCHITECTURE.md
│   └─► Проверка через 05_architecture_reviewer_prompt.md ✅
│
├─► Phase 3: Planning
│   ├─► Читаю System/Agents/06_agent_planner.md
│   ├─► Загружаю skill-tdd-stub-first
│   ├─► Создаю план в Stub-First формате:
│   │   • Task 1.1 [STUB]: Структура + stubs + тест на hardcode
│   │   • Task 1.2 [IMPL]: Реальная логика + финальные тесты
│   └─► Сохраняю в docs/PLAN.md ✅
│
└─► Phase 4: Development
    ├─► Читаю System/Agents/08_agent_developer.md
    ├─► Task 1.1 [STUB]:
    │   ├─► Создаю файлы: src/payments/service.py, models.py
    │   ├─► Методы возвращают hardcode
    │   ├─► Пишу тест: test_create_checkout_hardcoded()
    │   └─► [Tool: run_tests] → PASS ✅
    │
    ├─► Task 1.2 [IMPL]:
    │   ├─► Заменяю hardcode на Stripe API вызовы
    │   ├─► Обновляю тесты (убираю hardcode asserts)
    │   └─► [Tool: run_tests] → PASS ✅
    │
    └─► Code Review (09_agent_code_reviewer.md):
        └─► Финальная проверка → Approve ✅

✅ Готово! Все артефакты созданы, код работает, тесты зелёные.
```

**Вы не написали ни одной дополнительной команды.** Всё автоматически.

---

### Сценарий 2: Быстрый режим (LIGHT MODE)

Для простых задач (опечатки, мелкие UI правки):

```bash
claude
```

```
/light

Исправь опечатку в тексте кнопки: "Sumbit" → "Submit"
```

**Что происходит:**

```
Claude (AGENTS.md + skill-light-mode):
│
├─► Распознаю [LIGHT] задачу
├─► Пропускаю Architecture, Planning
├─► Выполняю только:
│   ├─► Minimal Analysis: Подтверждаю задачу
│   ├─► Development: Меняю текст кнопки
│   └─► Code Review: Быстрая проверка ✅
│
└─► Готово за 1-2 минуты (vs 15 минут в полном режиме)
```

---

### Сценарий 3: Восстановление сессии

```bash
# Вчера работали над фичей, сегодня продолжаем
claude
```

```
Продолжи работу над уведомлениями
```

**Claude автоматически:**

1. Читает `.agent/sessions/latest.yaml`:
   ```yaml
   TaskName: "Email Notifications System"
   TaskStatus: "IN_DEVELOPMENT"
   Mode: "DEVELOPMENT_PHASE"
   Summary: "Completed: Analysis, Architecture. Current: Development Task 2.1 [STUB]"
   ```

2. Восстанавливает контекст:
   ```
   Я вижу, мы на стадии Development, задача 2.1 [STUB] — создание email шаблонов.
   Продолжаю с этого места...
   ```

3. Продолжает работу без повторного анализа/архитектуры

---

## 🛠️ Доработка AGENTS.md под проект

### Когда НЕ нужно менять AGENTS.md

**В 95% случаев используйте файл как есть.**

AGENTS.md уже оптимизирован и содержит только критичную логику:
- Активация Orchestrator
- Загрузка TIER 0 skills
- Протокол Pipeline
- Session Restoration

### Когда МОЖНО изменить

**Только если:**

1. Вы используете кастомные Skills (добавили свои в `.agent/skills/`)
2. Вам нужен другой workflow по умолчанию
3. Вы хотите отключить Session Restoration (но зачем?)

**Пример добавления:**

```markdown
# В конец AGENTS.md после ## LIGHT MODE

## PROJECT-SPECIFIC SKILLS
- `skill-company-api-standards` — Our internal API conventions
- `skill-security-checklist-finance` — Finance-specific security rules

### Loading Protocol
These skills are loaded automatically in:
- Analysis Phase: `skill-company-api-standards`
- Security Audit: `skill-security-checklist-finance`
```

---

## 🎯 Правила работы с AGENTS.md

### ✅ DO:

1. **Копируйте как есть** из репозитория
2. **Создавайте симлинк** `CLAUDE.md → AGENTS.md` для Claude Code
3. **Коммитьте в Git** вместе с кодом
4. **Обновляйте** только при апдейте фреймворка

### ❌ DON'T:

1. **НЕ удаляйте секции** (каждая критична)
2. **НЕ добавляйте project-specific правила** (используйте отдельный файл)
3. **НЕ дублируйте** логику из `00_agent_development.md`
4. **НЕ отключайте** TIER 0 skills

---

## 📚 Связь AGENTS.md с остальными файлами

```
AGENTS.md (79 строк)
├─► Ссылается на:
│   ├─► System/Agents/00_agent_development.md  (Глобальные правила)
│   ├─► System/Agents/01_orchestrator.md       (Логика оркестрации)
│   └─► System/Agents/02-10_*.md               (Роли агентов)
│
├─► Загружает:
│   ├─► .agent/skills/skill-core-principles/   (TIER 0)
│   ├─► .agent/skills/skill-safe-commands/     (TIER 0)
│   ├─► .agent/skills/skill-artifact-management/ (TIER 0)
│   └─► .agent/skills/skill-session-state/     (TIER 0)
│
└─► Создаёт:
    ├─► .agent/sessions/latest.yaml            (Session context)
    ├─► docs/TASK.md                           (Спецификация)
    ├─► docs/ARCHITECTURE.md                   (Архитектура)
    └─► docs/PLAN.md                           (План)
```

---

## 🎓 Итоговое понимание

### AGENTS.md — это НЕ конфигурация, это АКТИВАТОР

**Аналогия:**

```
Обычный Claude Code:  Один разработчик пишет код
                      ↓
AGENTS.md:            Активирует команду из 10 специалистов
                      ↓
                      Analyst → Architect → Planner → Developer → Reviewer
```